{
  "name": "tabre-nepali-date",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "copy-extension": "node copy-dist-to-extension.mjs",
    "build-extension": "vite build && npm run copy-extension",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {
    "bootstrap-icons": "^1.13.1"
  }
}
